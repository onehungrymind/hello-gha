name: 'My Custom Action'
description: 'Hello Github Actions'

inputs:
  name:
    description: 'The name of the person to greet'
    required: true
    default: 'World'

runs:
  using: 'node20'
  main: 'dist/index.js'
